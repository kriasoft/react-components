/**
 * @jsx React.DOM
 */

var react = require('react');

var Input = React.createClass({
  render() {
    return (
      <div className="input">
        <input type="text" />
      </div>
    );
  }
});